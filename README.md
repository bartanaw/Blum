# Blum

This repository includes a simple scam detection website. Open `index.html` in your browser and paste any suspicious email, message or website text. The page will highlight whether the text looks suspicious based on common scam keywords and URL patterns.

No server is required; it is a static website. The detection logic is in `script.js` and uses heuristics only, so results may not be perfect.
